BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[ExamAttempts] ADD [lastHeartbeat] DATETIME2;

-- AlterTable
ALTER TABLE [dbo].[Users] ALTER COLUMN [password] VARCHAR(500) NOT NULL;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
